# 티켓 산출물 3종

티켓을 받으면 **무엇을 만들지부터 정한다.** 유형이 아니라 산출물이 절차를 바꾼다.
근거는 `design/decisions/0008-three-outputs-not-six-types.md`.

| 산출물 | 이런 티켓 | 표본 비중 |
|---|---|---|
| 작업 지시서 | 작업 대행, 사전 지원 요청 | 36% |
| 조사 결과 + 답변 | 장애·트러블슈팅, 비용·청구, 보안 사고 | 41% |
| 답변 + 출처 | 자문·문의 | 23% |

---

## 1. 작업 지시서

**고객 회신이 주산출물이 아니다.** 회신은 보통 "설정했습니다" 한 줄이고,
정작 필요한 건 실행하는 사람이 **그것만 보고 콘솔에서 끝낼 수 있는 목록**이다.
티켓 원문을 다시 읽게 만들면 실패다.

### 반드시 들어갈 것

- **대상** — 리소스명 또는 ID, 계정 ID, 리전. 이름만 있고 계정이 없으면 그것부터 되묻는다
- **값** — 바꿀 설정을 그대로. "적절히"나 "기존과 동일하게" 금지
- **기간** — 한시적이면 시작·종료와 **종료 후 원복 여부**
- **확인 항목** — 아래 4개는 매번 본다

```
[ ] 프로덕션 대상인가
[ ] 되돌리는 방법이 있는가
[ ] 기간 종료 후 원복이 필요한가
[ ] 요청에 없지만 같이 건드려야 하는 것이 있는가
```

### 에이전트가 하는 일

- **요청 분해** — 한 티켓에 요청이 여러 건 섞여 온다. 번호를 붙여 나눈다
- **누락 지적** — 실행 직전에 막힐 정보를 미리 찾는다
- **위험 표시** — 프로덕션·비가역·과금 영향

답변 문장을 길게 쓰는 것이 아니다. **실행 가능한 목록을 만드는 것**이다.

---

## 2. 조사 결과 + 답변

장애와 비용이 같이 묶이는 이유는 둘 다 **"왜 이렇게 됐나"를 증명**해야 하기 때문이다.

- 조회는 read-only 만. 실제 값(리소스ID·버전·지표·금액)을 답변에 옮긴다
- 근거마다 `[확인] [추측] [모름]` 을 붙인다. `[확인]` 은 이번에 실제로 본 것만
- 원인을 못 찾았으면 못 찾았다고 쓴다. 그럴듯한 추정으로 채우지 않는다
- **비용 건은 고객이 이미 화가 났거나 당황한 상태로 온다.** 사실 나열 전에 상황 인지를
  먼저 적는다. 톤은 `reply-style.md` 를 따른다
- 고객에게 나가는 금액은 **FitCloud 정제값만**. 원본 AWS 청구 수치 금지

---

## 3. 답변 + 출처

조회할 리소스가 없어서 **지어내기 가장 쉬운 유형**이다. 에이전트가 제일 잘하는 동시에
제일 위험하다.

- 사양·지원 범위·EOL 같은 주장은 **공식 문서를 이번에 열어서** 확인한다. 기억은 근거가 아니다
- 출처 URL 과 확인한 날짜를 남긴다
- 확인 못 한 것은 `[추측]` 으로 명시하고, 확정 답변에 쓰지 않는다
- 문서에 없으면 "문서에 없다"가 답이다. 채우지 않는다

---

## 어디에 적나

`tickets/<번호>.md` 한 파일 안에 적는다. 유형에 따라 쓰는 섹션이 다르다.

| 산출물 | 쓰는 섹션 |
|---|---|
| 작업 지시서 | `## 작업 지시` |
| 조사 결과 + 답변 | `## 확인한 것` → `## 답변 초안` |
| 답변 + 출처 | `## 확인한 것`(출처 포함) → `## 답변 초안` |

안 쓰는 섹션은 지운다. 빈 제목만 남겨두지 않는다.
