# Agent Runtime Capability Status

`as_of: 2026-08-25 KST`

This file records observed runtime readiness. Re-run the checks before relying on it; it is status, not permanent policy.

## Temporarily blocked capabilities

| Capability | Status | Reason | Re-enable gate |
|---|---|---|---|
| `customer-aws-readonly` | blocked | `aws-customer-account-ops` has a known issue and is being fixed by its developer | Install a fixed release, audit it, verify broker-side read-only enforcement, then run a synthetic smoke test |
| `fitcloud-billing` | blocked | Current implementation is bundled with the same affected skill | Verify the fixed wrapper and FitCloud-only customer-output gate before use |

Do not install, invoke, or copy the affected skill until the re-enable gate passes. Its presence in another agent's home directory does not make it approved.

## Hermes AWS official-research capability

| Item | Status | Verified configuration |
|---|---|---|
| Upstream | verified | `aws/agent-toolkit-for-aws` commit `904bfa14cc32e589e427f3dbf68c1a2ed6bd3830` |
| Local skill | enabled | `~/.hermes/skills/aws-security/`; 24 files; installed tree matched audited upstream |
| MCP server | enabled | `aws-docs` via `uvx mcp-proxy-for-aws@1.6.4` and the managed AWS MCP endpoint |
| Authentication | documentation-only | `--skip-auth`; no AWS credential passed to the MCP proxy |
| Connection test | passed | 9 tools discovered; 4 selected |
| Documentation smoke test | passed | `search_documentation` and `read_documentation` retrieved current AWS documentation for Aurora cluster snapshot encryption |

### Enabled MCP tools

- `aws___search_documentation`
- `aws___read_documentation`
- `aws___list_regions`
- `aws___get_regional_availability`

### Disabled MCP tools

- `aws___call_aws`
- `aws___run_script`
- `aws___get_tasks`
- `aws___get_presigned_url`
- `aws___retrieve_skill`

The disabled tools must remain unavailable in the `tiket` workflow. Documentation access does not grant customer-account access.

## Selected local AWS skills

Only `aws-security` was installed from the upstream toolkit because its complete source explicitly limits procedures to read-only APIs and contains no executable scripts. The full `aws-core` bundle was not installed.

Explicitly excluded for now:

- `aws-billing-and-cost-management`
- `aws-cdk`
- `aws-cloudformation`
- `aws-deployment`
- `launch-with-aws`
- other provisioning or mutation-oriented skills

Add a service-specific skill only after a ticket requires it and its source has been audited against repository boundaries.

## Verification commands

```bash
hermes skills list
hermes mcp list
hermes mcp test aws-docs
hermes tools list
python3 scripts/validate_workspace.py
```

A newly installed skill or MCP requires a new Hermes session before use.
